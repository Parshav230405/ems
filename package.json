{
  "name": "aura-ems",
  "version": "1.0.0",
  "description": "AURA EMS - Enterprise Multi-Tenant Education Management System",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install --include=dev && npm run build && npx prisma db push --accept-data-loss --force-reset",
    "start": "cd backend && npm start",
    "render:build": "npm run build && cd backend && npm run prisma:seed:clean",
    "db:push": "cd backend && npx prisma db push --accept-data-loss --force-reset",
    "db:seed": "cd backend && npm run prisma:seed:clean",
    "db:seed:demo": "cd backend && npm run prisma:seed",
    "postinstall": "cd backend && npm install --include=dev"
  }
}
